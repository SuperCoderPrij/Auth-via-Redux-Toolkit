# Auth-via-Redux-Toolkit
Test Authentication setup using Redux toolkit. I also used hydration so that on every reload redux toolkit has the old data and doesn't resets on every reload. And I use localstorage for it ;) 
